db:
  db_user: "postgres"
  db_pass: "postgres"
  db_name: "postgres"
  db_port: "5432"
  ssl_mode: "disable"
server:
  timeout: 10s
  idle_timeout: 30s
  address: "0.0.0.0:8888"
nats:
  address: "nats://localhost:8887"
  retry: Yes
  max_reconnects: 10
  reconnect_wait: 10s
